# 意思決定の記録

いつ、何を、なぜ決めたか。新しいものを上に積む。

**この文書は履歴。** いま何が正なのかは `DESIGN_SYSTEM.md` を見る。
規範が変わったらそちらを更新し、ここには経緯を残す。

書くのは「判断が要ったこと」だけ。作業ログではない。
**選ばなかった案とその理由を必ず書く** — 間違ったほうは単独で見ると
たいてい妥当に見えるので、それが分からないと同じ道を辿る。

---

## 2026-08-17 メニューのコマンドをひらがなに開く

メニューの項目を「これまで / きかれたこと / はなしたこと / かいたもの /
できること / はなしかける」に。5つを「〜たこと」で揃えた。

漢字の名詞（経歴・取材・登壇）は資料の目次の語で、並べると会社案内の
ナビになる。「はなしかける」は言葉遊びではなく、RPG のそのコマンドが
指す動作とセクションの用途が同じ。

- 却下: **英語のみ** — ゲームのメニューに英語は並ばない。「つよさ」の
  隣に "Status" とは書かない
- 却下: **英語＋日本語の併記** — 短さが本体のパネルで単語数が倍になる。
  桁を揃えるために言葉も歪んだ（「連絡」はあの列以外で誰も書かない）
- 却下: **漢字2文字で統一** — 4つはたまたま2文字が自然だっただけ。
  Services と Contact を桁に押し込んでいた
- **Rail は英語のまま**。「いまどこか」を見出しと対応させて示す
  インジケータで、コマンド窓とは役割が違う。同じ画面に両方は出ない

→ 規範: DESIGN_SYSTEM.md「移動の言葉は、役割で書き分ける」

## 2026-08-17 破線をカード裏だけに戻す

Services とプロフィール文の区切りを実線に変更。破線はカード裏の
RPG ステータス画面（CLASS / ゲージ / STRENGTHS の仕切り）だけに。

Services は破線、Speaking は実線という状態になっていた。どちらも
同種の項目の並びなのに違う。その場その場で選んだ結果で、意味を
持たせていなかった。

→ 規範: DESIGN_SYSTEM.md「破線はステータス画面の中だけ」

## 2026-08-17 枠を html の背景にし、スクロールを内側へ

`#root` を `position: fixed; inset: var(--frame)` にして内側だけを
スクロールさせる。枠は「描いた線」ではなく背景と穴の関係になった。

ビューポート固定のオーバーレイ（`box-shadow: 0 0 0 100vmax`）で
描いていたが、枠が画面に浮いている限り3つの穴が残る。

1. ページ高さが変わってスクロールバーが出入りすると右端がずれる
2. モバイルで URL バーが伸縮すると下端が動く
3. 巨大な影がスクロール中に再描画される

副作用として `window.scrollY` 依存の4箇所を書き換え。スクロール検知は
コンテナ・window・document の3つに登録した（スクロールする層が
変わるのはこれで2度目なので、次に備える）。

→ 規範: DESIGN_SYSTEM.md「枠は描かない、背景として置く」

## 2026-08-15 ページを画面として枠取る

`body` を炭色にし、`#root` を角丸で切り抜く。ページが「画面」、
周囲が「筐体」になる。thin / charcoal / 意匠なしで確定。

Zone 1 だけがゲームのタイトル画面で、Zone 2 はただのリストだった。
サイト全体に外殻を与えるのが目的。

- 却下: 枠に電源ランプや型番のような刻印を置く — 1つでも寒くなる
- 却下: 太い枠（本体感は出るが画面が狭くなる）

同時に、狭い画面用の SectionMenu を追加。Rail は 1280px 以上でしか
出ないので、それ未満に移動手段がなかった。

## 2026-08-15 Rail と年フィルタを本採用

Rail は右・固定・HUD と同じタイミングで出す。判定は
`useScrolledPastHero` に一本化した（別々に閾値を持つといつかズレる）。

年フィルタは Speaking だけ。Interviews は3件・2年しかなく、チップの
ほうが中身より多くなる。**絞り込んでも全件は DOM に残す** —
`hidden` で隠すだけなので、エージェントからは常に10件読める。

## 2026-08-14〜15 デザインラボで41案を検証、5案採用

`?lab` で見た目を切り替えられる仕組みを作り、案を出しては捨てた。
採用は Menu / HUD / Rail / SectionMenu / 枠取りの5つ。

**通ったのは全部「操作できるもの」**、通らなかったのは全部「見た目を
借りただけのもの」だった。この基準は次に何か足すときにも使える。

却下した41案の一覧は末尾に。

## 2026-08-14 コントラストと見出しを直し、判子を廃止

- `--color-text-muted` を `#7A7870` → `#6E6C64`。WCAG AA 未達（4.20）
  だったのに 12px で40箇所以上使われていた。一番小さい文字が一番
  読みにくい色だった
- セクション見出しを 14px → 18px。本文と同サイズで、赤い縦棒がなければ
  見出しと分からなかった
- フォーム入力欄を 16px。iOS Safari は 16px 未満でフォーカスすると
  ページを自動ズームする
- 判子（`.hanko`）を廃止。仕様に「唯一記憶に残る要素」と書かれながら
  一度も描画されず、CSS だけが残っていた。仕様自身が「手描き SVG で
  ないと意味がない」と述べており、コードで再現する前提と噛み合わない

**却下: 全体の文字サイズを一段ずつ上げる。** 階層が2段に潰れている
（12px と 14px で73箇所中69）のは事実だったが、全部を上げても比率は
変わらず、120% 拡大したように見えるだけだった。足りないアンカーを
1つだけ足すのが正しかった。

→ 規範: DESIGN_SYSTEM.md「文字サイズは 12px をメタ情報に留める」
  「コントラストは測ってから決める」

## 2026-08-14 microCMS を廃止

登壇・取材を `src/data/*.json` の直接編集に移行。画像13枚も
`public/images/` に自前ホスト（4.98MB → 1.3MB）。

更新が年3〜4件しかないのに、API キーと外部サービスをビルド経路に
置いていた。一方で頻繁に更新される執筆記事は note の公開 RSS
（無料・鍵不要）で回っていた。逆転していた。

同時に全13件へサマリを追加。JSON-LD の Event / Article に
`description` が1つも無く、「どんなテーマで話す人か」に答える材料が
タイトルの羅列しか無かった。

## 2026-08-14 引用用プロフィール文を用意

50 / 100 / 150 / 200 / 250 / 300 字の6本。コピーボタン付きで
Experience の末尾に折りたたむ。

長さだけでなく粒度で分けた。50 と 100 はキャリアに触れず「いま何を
しているか」、150 以上が経緯を持つ。年号は 200 字以上にだけ入れる
（短いものが古びない）。

llms.txt にも「要約を新たに生成せず、そのまま引用してください」と
添えた。AI が紹介文を書くとき、実際の仕事を「OMOプラットフォーム」
「DX SaaS」のような抽象語に丸めてしまうため。

- 却下: 「大企業の変革と内製化を専門に」— 31件中10件がその主題
  だったが、**話す頻度と専門を名乗ることは別**。自分で渡す紹介文で
  名乗ると事実より大きく出る

## 2026-08-14 クローラーとエージェントに読ませる

`robots.txt` を全ブロックから全許可へ。ビルド時プリレンダリングを
導入し、`llms.txt` / `sitemap.xml` / `/data/*.json` を配信。

CSR オンリーだったため、JS を実行しない取得では body が空の div
だけだった。JSON-LD も `useEffect` で注入していたので一緒に消えていた。

非JS取得の本文: 0字 → 3,800字。

---

## 却下した案の一覧

デザインラボで検証して採用しなかったもの。同じ地面を二度歩かないため。

**構成・情報設計**
Timeline（4セクションを1本の年表に統合）/ Themes（形式でなく主題で
束ねる）/ Ask（「何に困っていますか」から入る）/ Thesis（主張を先に
置く）/ Reverse（Services を先頭に）/ Canon（代表作3本）/
Merge（Interviews を Speaking に統合）

**見た目**
Console（等幅のステータス画面風）/ Sheet（見出しを左列に送る2カラム）/
Dark（ダークモード）/ Mincho（明朝体）/ Paper（純白＋硬質な線）/
Wide（カラム 820px）

**機能の追加**
Keys（↑↓ で選択）/ Counts（見出しに件数）/ Fresh（NEW バッジ）/
Progress（読み進みバー）/ Find（絞り込み入力）/ Sort（古い順）/
Anchor（行ごとのリンク）/ Material（資料ありで絞る）/
UrlState（絞り込みを URL に）/ Top（先頭へ戻る）/ Nav（HUD に
セクション一覧）/ Crumb（HUD に現在地名）/ Articles（note 18本を
一覧表示）/ Thumbs（行からサムネイル）/ Output（経歴に発信件数）/
AllOpen（最初から全件）/ Sticky（見出しを貼り付け）/ Cue（▼ scroll）

**演出**
Encounter（「◯◯があらわれた！」）/ Save（ぼうけんのしょ）/
LevelUp（読み進むと年が上がる）/ Skills（主題をゲージで）/
Party（経歴をなかま欄に）/ Command（Contact をコマンド窓に）/
Wipe（遷移時のフラッシュ）/ Shrink（カードが縮む）
